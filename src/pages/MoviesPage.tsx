import React from 'react';

import {MoviesList, Theme} from "../components";

const MoviesPage = () => {
    return (
        <div>
            <MoviesList/>

        </div>
    );
};

export {MoviesPage}