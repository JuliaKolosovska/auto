import React from 'react';

import {MoviesList, ThemeSwitcher} from "../components";

const MoviesPage = () => {
    return (
        <div>
            <MoviesList/>

        </div>
    );
};

export {MoviesPage}